;;; early-init.el --- Early startup settings -*- lexical-binding: t; -*-

;; Keep Emacs from eagerly loading package.el before init.el decides what to do.
(setq package-enable-at-startup nil)

;;; early-init.el ends here
