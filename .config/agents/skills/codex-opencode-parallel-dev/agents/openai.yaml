interface:
  display_name: "Any-Model OpenCode Parallel Dev"
  short_description: "Run parallel OpenCode workers via Herdr from any orchestrating model"
  default_prompt: "Use $codex-opencode-parallel-dev to split this development task across safe OpenCode workers and integrate the results, orchestrating from any model."
